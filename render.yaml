services:
  - type: web
    name: cis-sherlock
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn cis_server:app --timeout 660 --workers 1 --threads 4 --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
